# Prompt Log

## Задание С3: Написать Dockerfile для Rust-приложения

### Промпт 1
**Инструмент:** Codex (GPT-5)
**Промпт:** "Создай простой Rust-сервис для лабораторной работы №11. Нужен HTTP-сервер с `/health`, `/shared` и `/write`, который использует общий volume для записи файлов и может собираться в Docker."
**Результат:** Получен каркас Rust-приложения без внешних web-фреймворков, чтобы сборка была компактной и предсказуемой.

### Промпт 2
**Инструмент:** Codex (GPT-5)
**Промпт:** "Сделай Dockerfile для Rust так, чтобы он использовал многоэтапную сборку и максимально хорошо кешировал слои при изменении исходников."
**Результат:** Подготовлен multi-stage Dockerfile: сначала копируется `Cargo.toml`, затем исходники, а финальный образ остаётся минимальным.

## Задание С5: Создать docker-compose.yml, поднимающий Python, Go и Rust сервисы

### Промпт 1
**Инструмент:** Codex (GPT-5)
**Промпт:** "Собери docker-compose.yml для трёх сервисов на Python, Go и Rust. Нужны отдельные порты, общая сеть, healthcheck и единая точка для обмена данными."
**Результат:** Добавлен `docker-compose.yml` с тремя сервисами, общей сетью и общим volume.

### Промпт 2
**Инструмент:** Codex (GPT-5)
**Промпт:** "Проверь, чтобы сервисы запускались в правильном порядке и чтобы Go-контейнер был доступен другим сервисам до старта Python и Rust."
**Результат:** В Compose добавлен `depends_on` по состоянию `service_healthy` для упорядоченного старта.

## Задание С7: Использовать volume для обмена данными между контейнерами

### Промпт 1
**Инструмент:** Codex (GPT-5)
**Промпт:** "Добавь общий volume для трёх контейнеров и сделай так, чтобы каждый сервис писал в него свой файл и мог читать файлы остальных."
**Результат:** Все сервисы получили доступ к `/shared`, а в ответах `/shared` отображается содержимое общего каталога.

### Промпт 2
**Инструмент:** Codex (GPT-5)
**Промпт:** "Сделай так, чтобы запись в volume происходила автоматически при старте контейнера, чтобы обмен данными был виден сразу после запуска compose."
**Результат:** В каждом сервисе добавлена инициализация, создающая служебный файл при запуске.

## Задание П3: Настроить CI/CD, который собирает и пушит образы для всех трёх языков

### Промпт 1
**Инструмент:** Codex (GPT-5)
**Промпт:** "Создай GitHub Actions workflow, который собирает Docker-образы Go, Python и Rust и пушит их в GitHub Container Registry только при push в основную ветку."
**Результат:** Подготовлен workflow с matrix-сборкой, логином в GHCR и публикацией образов на push.

### Промпт 2
**Инструмент:** Codex (GPT-5)
**Промпт:** "Добавь к workflow кэширование слоёв сборки, чтобы повторные запуски были быстрее."
**Результат:** В workflow включены `cache-from` и `cache-to` через GitHub Actions cache backend.

## Задание П5: Оптимизировать слои Docker-образов для максимального кэширования

### Промпт 1
**Инструмент:** Codex (GPT-5)
**Промпт:** "Оптимизируй Dockerfile для Go, Python и Rust так, чтобы зависимости ставились отдельно от копирования исходников."
**Результат:** Во всех Dockerfile зависимости и конфигурация копируются раньше исходного кода, что улучшает повторное использование слоёв.

### Промпт 2
**Инструмент:** Codex (GPT-5)
**Промпт:** "Добавь `.dockerignore` и `.gitignore`, чтобы в build context не попадали локальные кэши, артефакты и временные файлы."
**Результат:** Исключены `target/`, `.venv/`, `__pycache__/`, `coverage` и прочие временные файлы.

## Итог

- Всего промптов: 10
- Что дополнительно проверено вручную: структура каталогов, отсутствие лишних артефактов в рабочем дереве, читаемость README и Compose-конфигурации.
- Время: ~90 минут
